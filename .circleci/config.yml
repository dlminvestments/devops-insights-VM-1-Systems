orbs:
  codecov: codecov/codecov@3.2.4
  jobs:
  example-job:
    docker:
      # replace with your preferred image
      - image: cimg/base:stable
    steps:
      - codecov/upload
workflows:
  example-workflow:
    jobs:
      - example-job